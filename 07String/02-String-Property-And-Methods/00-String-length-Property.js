// Properties : 
// => Means string ka apne internal variable jo kuch value store krega

// (a) length : 
console.log("Length of String is : " + str0.length); // calculate the length of the string (including space)
console.log("Length of string is : " + str1.length); // calculate the length of the string (including space)
console.log();

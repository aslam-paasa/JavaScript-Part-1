/**
 * Local Storage:
 * Data is not persistent means it is not stored anywhere in the browser.
 * So, whenever we refresher we lost all the data.
 * 
 * Q. How to make our data persistent?
 * => Local Storage: It is a window property(browser's feature) available
 *    in the browser where  we can store key-value pairs to make data
 *    persistent(permanent).
 * => Max Storage supported is upto 10MB. 
 * */ 
// How to get only value from object?
// Use object.value() method -> returns array of object's values
Object.keys(student).forEach((values) => console.log(`values is ${values}`));
console.log(Object.values(student));

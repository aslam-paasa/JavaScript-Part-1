/* Function with parameter and return value :
 - A function can also have one or more parameters and return values. For example,
   the function square defined below has a single parameter called number and 
   returns the square of the number.
*/ 

function square(number) {
    return  number * number;
}

// When we do not explicitly return any value from the function, undefined is
// returned by default.
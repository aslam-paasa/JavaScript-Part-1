/**
 * Unary minus : (-)
 * => It also converts the operand to a number, but always make the result after
 *    negating it (negative). 
*/

let x = "22";
y = -x;
console.log(typeof x); // String
console.log(typeof y); // Number
console.log(y); // -22

/**
 * => If this unary minus & plus is not able to convert the given operand into
 *    a valid number then it will return an invalid number i.e. NaN.
 * */


/**
 * Other unary operators :
 * 1. Logical NOT (!)
 * 2. typeOf
 * */ 

// at(): This method returns the element at the specified index in the array.
// It takes one argument: the index.

const arr = ['apple', 'banana', 'cherry'];
console.log(arr.at(1));  // Output: 'banana'


// Accesses an element at a specific index 
// (Note: This method doesn't exist for arrays; it's used with strings and objects).
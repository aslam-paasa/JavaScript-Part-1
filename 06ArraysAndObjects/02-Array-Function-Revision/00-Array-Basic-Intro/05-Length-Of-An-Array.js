let person = {
    name: "Piyush",
    age: "25",
}
let arr04 = ["apple", "banana", "cherry", person];

// If we have an array and we don't know how many elements are inside of this
// array. How we are gonna know it?
// => We have a property called "length" property.

console.log(arr04.length); // 4 elements inside this array
const dummy = [2, 5, 1, 6];

 // It's going to replace everything inside of an array with the value 
 // we provided in between these brackets over here.

dummy.fill(0);
console.log(dummy); // [ 0, 0, 0, 0 ]
/*
Problem : Given an array of integers, create a new array that will have
double of each element in the first array and then log it in the console.
*/ 

const arr10 = [1,2,3,4,5]

let doubleSum = arr10.map(item => item * 2);
console.log(doubleSum);
